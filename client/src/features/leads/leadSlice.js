// Lead local state — currently empty.
// All lead data is managed via RTK Query (leadApi.js).
// Add reducers here only for local UI state (e.g., selected lead, filters)
// that does NOT need to be fetched from the server.
