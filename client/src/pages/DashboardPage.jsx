import DashboardPageAnalytics from "../features/dashboard/DashboardPage";

/**
 * DashboardPage — keeps the existing /dashboard route.
 * Phase 4.6 renders the Analytics Dashboard.
 */
const DashboardPage = () => <DashboardPageAnalytics />;

export default DashboardPage;

